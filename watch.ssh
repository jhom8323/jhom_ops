watch -n 5 -d "echo Slave;echo stats | nc `echo $HOSTNAME | sed s/-a-/-b-/` 11211 | grep -e vb_active_num_non_resident -e \ curr_items\  -e min_data_age -e cmd_set -e ep_inconsistent_slave_chk; echo; echo Master;echo stats | nc `echo $HOSTNAME | sed s/-b-/-a-/` 11211 | grep -e vb_active_num_non_resident -e \ curr_items\ ;echo; echo Replication Tap;echo stats\ tap | nc `echo $HOSTNAME | sed s/-b-/-a-/` 11211 | grep -e :queue_backfillremaining -e backfill_completed"
